import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  footer: {
    marginTop: "auto",
  },
  footerText: {
    color: "white",
  },
}));

export { useStyles };
