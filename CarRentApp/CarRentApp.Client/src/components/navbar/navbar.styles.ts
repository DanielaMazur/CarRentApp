import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  logo: {
    color: "white",
    fontWeight: "bold",
    cursor: "pointer",
  },
  loginButton: {
    marginLeft: "auto",
  },
});

export { useStyles };
