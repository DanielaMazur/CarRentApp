import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  logo: {
    color: "white",
    fontWeight: "bold",
    cursor: "pointer",
  },
  navbarIcon: {
    color: "white",
  },
  navbarButton: {
    margin: "0 10px",
  },
});

export { useStyles };
