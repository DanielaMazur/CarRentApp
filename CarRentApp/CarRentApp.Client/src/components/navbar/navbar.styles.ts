import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  logo: {
    color: "white",
    fontWeight: "bold",
    cursor: "pointer",
  },
  reservationsIcon: {
    color: "white",
  },
  reservationsButton: {
    marginRight: "10px",
  },
});

export { useStyles };
