import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  imageContainer: {
    width: "100%",
    minHeight: "200px",
  },
  card: {
    cursor: "pointer",
  },
}));

export { useStyles };
