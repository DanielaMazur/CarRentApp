import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  imageSlider: {
    width: 600,
  },
  container: {
    display: "flex",
    marginTop: 20,
  },
}));

export { useStyles };
